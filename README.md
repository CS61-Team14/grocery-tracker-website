# Grocery Tracker Website

## How to Use
Clone this repo and then run `npm install` or `yarn install` in the command line to install all dependencies.
Do `npm run start` or `yarn start` to start the website.
